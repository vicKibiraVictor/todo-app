# ─────────────────────────────────────────────────────
#  06-app-metrics.yaml
#  FUTURE: Enable when you're ready to monitor
#  the todo-app backend and Postgres
#
#  Step 1 — Add prom-client to backend:
#    npm install prom-client
#    Then expose GET /metrics in index.js
#
#  Step 2 — Uncomment and apply this file:
#    kubectl apply -f k8s/monitoring/06-app-metrics.yaml
# ─────────────────────────────────────────────────────

# ── ServiceMonitor: scrape the backend /metrics endpoint ──
# apiVersion: monitoring.coreos.com/v1
# kind: ServiceMonitor
# metadata:
#   name: todo-backend
#   namespace: monitoring
#   labels:
#     release: kube-prometheus
# spec:
#   namespaceSelector:
#     matchNames: [todo-app]
#   selector:
#     matchLabels:
#       app: backend
#   endpoints:
#     - port: http
#       path: /metrics
#       interval: 15s

---

# ── Postgres Exporter: exposes DB metrics to Prometheus ──
# Uncomment the block below AND add this to prometheus-values.yaml:
#   additionalScrapeConfigs: ...
#
# apiVersion: apps/v1
# kind: Deployment
# metadata:
#   name: postgres-exporter
#   namespace: todo-app
# spec:
#   replicas: 1
#   selector:
#     matchLabels:
#       app: postgres-exporter
#   template:
#     metadata:
#       labels:
#         app: postgres-exporter
#     spec:
#       containers:
#         - name: postgres-exporter
#           image: prometheuscommunity/postgres-exporter:latest
#           env:
#             - name: DATA_SOURCE_NAME
#               value: "postgresql://postgres:postgres@postgres:5432/tododb?sslmode=disable"
#           ports:
#             - containerPort: 9187
#               name: metrics
# ---
# apiVersion: v1
# kind: Service
# metadata:
#   name: postgres-exporter
#   namespace: todo-app
#   labels:
#     app: postgres-exporter
# spec:
#   selector:
#     app: postgres-exporter
#   ports:
#     - name: metrics
#       port: 9187
# ---
# apiVersion: monitoring.coreos.com/v1
# kind: ServiceMonitor
# metadata:
#   name: postgres-exporter
#   namespace: monitoring
#   labels:
#     release: kube-prometheus
# spec:
#   namespaceSelector:
#     matchNames: [todo-app]
#   selector:
#     matchLabels:
#       app: postgres-exporter
#   endpoints:
#     - port: metrics
#       interval: 30s
